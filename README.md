# Summary

This is an example project showing the differences between creating the same page using shadcn vs base-ui

# Commands

run project (separately both for shadcn and base-ui)

```
npm run dev
```

run storybook (only configured for shadcn)

```
npm run storybook
```
